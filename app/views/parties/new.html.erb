<div class="container">
  <%= form_for @party do |party| %>
    <p> Group Name </p>
    <%= party.text_field :name, placeholder:'Pixie Chix', maxlength: 50 %>
      <%= hidden_field(:party, :user_id, :value => current_user.id) %>
      <br>
      <%= button_to 'Start Your Group', nil, :class => 'btn btn-large red darken-2' %>
  <% end %>
</div>
